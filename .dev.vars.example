# Yerel geliştirme secret'ları.
#
# NOT: Ortam (sandbox/production) BURADA TANIMLANMAZ. Her istek ortamını kendisi
# taşır (imzalı gövde alanı, connect parametresi, session token claim'i) ve eklenti
# ona göre cevap verir. Bu dosyanın kopyasını `.dev.vars` olarak oluştur.
# .dev.vars ASLA commit edilmez (.gitignore'da).
RESTOMENUM_PLUGIN_ID=""
RESTOMENUM_CLIENT_SECRET="cs_..."
# base64 kodlu 32 bayt:  openssl rand -base64 32
SECRET_ENCRYPTION_KEY=""
